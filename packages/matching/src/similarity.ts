import type { JobPosting, JobSimilarityResult } from "@ghostboard/shared";

/** TODO(team)[matching]: implement embedding cosine similarity or keyword overlap. */
export function calculateJobSimilarity(target: JobPosting, candidates: JobPosting[]): JobSimilarityResult[] {
  void target;
  void candidates;
  return [];
}
