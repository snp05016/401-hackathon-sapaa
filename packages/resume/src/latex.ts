/** TODO(team)[resume]: real LaTeX diffing (line-level or section-level) between original and tailored output. */
export function diffLatex(before: string, after: string): string[] {
  void before;
  void after;
  return [];
}
