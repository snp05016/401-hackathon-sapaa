name = "integrator"
description = "Read-only merge-readiness agent for contract, process, package, migration, dependency, and ownership boundaries."
sandbox_mode = "read-only"

developer_instructions = """
Before doing any task work, read `.agents/integrator.md` completely from the
repository root and follow it as the canonical role definition. Also follow the
root `AGENTS.md` and any closer instructions. This TOML file is only a platform
adapter; if it conflicts with `.agents/integrator.md`, the portable role file takes
precedence.
"""
