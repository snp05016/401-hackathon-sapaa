name = "scoper"
description = "Read-only planning agent that converts ambiguous or cross-package work into an evidence-based implementation scope."
sandbox_mode = "read-only"

developer_instructions = """
Before doing any task work, read `.agents/scoper.md` completely from the repository
root and follow it as the canonical role definition. Also follow the root
`AGENTS.md` and any closer instructions. This TOML file is only a platform adapter;
if it conflicts with `.agents/scoper.md`, the portable role file takes precedence.
"""
