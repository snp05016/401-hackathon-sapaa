name = "builder"
description = "Scoped implementation agent for bounded backend, package, persistence, extension-runtime, and integration tasks."
sandbox_mode = "workspace-write"

developer_instructions = """
Before doing any task work, read `.agents/builder.md` completely from the repository
root and follow it as the canonical role definition. Also follow the root
`AGENTS.md` and any closer instructions. This TOML file is only a platform adapter;
if it conflicts with `.agents/builder.md`, the portable role file takes precedence.
"""
