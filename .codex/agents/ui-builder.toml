name = "ui_builder"
description = "Scoped UI implementation agent for accessible Electron desktop and Chromium extension interfaces."
sandbox_mode = "workspace-write"

developer_instructions = """
Before doing any task work, read `.agents/ui-builder.md` completely from the
repository root and follow it as the canonical role definition. Also follow the
root `AGENTS.md` and any closer instructions. This TOML file is only a platform
adapter; if it conflicts with `.agents/ui-builder.md`, the portable role file takes
precedence.
"""
