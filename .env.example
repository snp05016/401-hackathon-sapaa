GHOSTBOARD_LLM_PROVIDER=groq
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b
GEMINI_API_KEY=
GHOSTBOARD_BRIDGE_PORT=4173
JOBSPY_URL=http://127.0.0.1:8001

# Optional: bundle a Google Desktop-app OAuth client so the recruiter inbox
# connects with one click instead of asking each user for a credentials JSON.
# Create one at console.cloud.google.com > Credentials > OAuth client ID >
# Desktop app, with the gmail.readonly scope.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
